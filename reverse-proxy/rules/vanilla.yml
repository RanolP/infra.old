tcp:
  routers:
    vanilla:
      entryPoints:
        - 'minecraft-java'
      rule: 'HostSNI(`play.ranol.tk`)'
      service: 'vanilla'
  services:
    vanilla:
      loadBalancer:
        servers:
          - address: ':25000/tcp'
